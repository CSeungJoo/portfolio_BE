spring:
  application:
    name: profile
  profiles:
    active: dev
  config:
    import: optional:configserver:http://${username}:${username}@localhost:8888

security:
  username: ${username}
  password: ${username}